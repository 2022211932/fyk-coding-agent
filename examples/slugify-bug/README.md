# Slugify Demo Task

This deliberately incomplete project is the input for a Yukai demo.

Task:

> 修复 `slugify`，使全部测试通过；不要修改测试，并在结束前运行完整测试。

Run the initial failing tests with:

```text
python -m unittest -v
```
